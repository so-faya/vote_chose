import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/vote_chose/', // Add this line to ensure correct paths for GitHub Pages deployment
})
